import  as React from 'react';

interface EmailTemplateProps {
  firstName string;
}

export const EmailTemplate React.FCReadonlyEmailTemplateProps = ({
  firstName,
}) = (
  div
    h1Welcome, {firstName}!h1
  div
);
